---
labels: ["testing-library"]
description: "This testing-lib for mongodb memory server."
---

### To use this library just require this spec file, and your spec is connected to mongodb

Then just use the models normally.

```ts
require("@jamalsoueidan/bit-dev.testing-library.mongodb/mongodb.jest");
```

API:

```ts
function connect(): void;
function clearDatabase(): void;
function disconnect(): void;
```
