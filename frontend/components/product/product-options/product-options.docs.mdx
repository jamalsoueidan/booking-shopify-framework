---
description: A ProductOptions component.
---

A component that handle buffertime and duration fields.

### Component usage

```js
const { fields } = useForm({
  fields: {
    buffertime: useField(5),
    duration: useField(30),
  },
});

return <ProductOptions {...fields} />;
```
