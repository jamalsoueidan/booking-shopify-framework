---
description: A ProductStatus component.
---

A component that render a select field (active, deactivated)

### Component usage

```js
const field = useField(true);
return <ProductStatus staffLength={1} active={field} />;
```
