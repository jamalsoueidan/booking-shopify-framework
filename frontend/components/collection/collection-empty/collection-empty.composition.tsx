import { withApplicationCard } from "@jamalsoueidan/bit-dev.preview.with-application";
import React from "react";
import { CollectionEmpty } from "./collection-empty";

export const BasicCollectionEmpty = withApplicationCard(() => (
  <CollectionEmpty />
));
