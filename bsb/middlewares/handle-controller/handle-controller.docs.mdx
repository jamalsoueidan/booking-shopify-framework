---
labels: ["module", "express"]
description: "Handle controller"
---

API:

```ts
router.get("/url", handleController(controller.user));

// controller.user
export const user = async ({
  query,
  body,
  session,
}: ControllerProps<ShopQuery, UserReceivePasswordBodyRequest, Session>) => {
  // use query, body, sessoion
};
```
