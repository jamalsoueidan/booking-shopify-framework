---
description: "A React Hook that dispatch with prev, next value"
labels: ["hook"]
---

## A React Hook to get prev value

Increments the state of `count` by 1 each time `increment` is called

```js
usePrevious(
  ([prevDate]) => {
    console.log(prevDate, date);
  },
  [date]
);
```
