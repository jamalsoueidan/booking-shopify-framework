---
description: "A React Hook that show message on screen"
labels: ["use-toast", "hook"]
---

import { useToast } from "./use-toast";

## A React Hook to show message on screen

### Component usage

In this example we are showing the toast message straight away.

```js
import { useToast } from "./use-toast";

const toast = useToast();

useEffect(() => {
  toast.show({ content: "hej med dig" });
}, []);
```
