---
description: "A React Hook that translate tag-color to tag-text"
labels: ["use-tag", "hook"]
---

A component that return tag-text for the tag-color

### Component usage

```js
const { selectTag } = useTag();
return <>{selectTag("#4b6043")}</>;
```
