---
description: "A React Hook that modify modal props"
labels: ["hook", "modal"]
---

## A React Hook to modify modal props

```js
const { setPrimaryAction } = useModal();

useEffect(() => {
  setPrimaryAction({
    content: "Tilføj",
    onAction: () => {
      console.log("Add");
    },
  });
}, []);

return (
  <Modal.Section>MockComponent that read value from context</Modal.Section>
);
```
