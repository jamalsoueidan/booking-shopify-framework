---
description: A InputTimer component.
---

A component that render time

### Component usage

```ts
const field = useField < InputTimerFieldType > undefined;
return <InputTimer data={hours} {...field} />;
```
